# PINS MATTER HERE. fastmcp 3.x needs a modern starlette; fastapi 0.115.6 caps
# starlette at <0.42.0. Installing fastmcp against that fastapi silently upgraded
# starlette to 1.6.0 and app.py then failed to import at all with
# "Router.__init__() got an unexpected keyword argument 'on_startup'" — i.e. the
# REST API would have stopped booting in the deployed image. These four are the
# set pip resolves to together; do not bump one alone.
fastapi==0.141.1
uvicorn[standard]==0.52.3

# MCP endpoint mounted at /mcp/ by app.py. The mount is import-guarded, so
# omitting these only disables /mcp — the REST API still serves. Kept in sync
# with requirements-mcp.txt, which is the standalone/stdio install.
fastmcp==3.4.7
httpx==0.28.1
